# Postgres backend for local/dev. Start with: make devenv sources=postgres
# Credentials match devenv/docker/blocks/postgres/docker-compose.yaml
[database]
type = postgres
host = 127.0.0.1:5432
name = grafana
user = grafana
password = password
ssl_mode = disable
